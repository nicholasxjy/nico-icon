/* eslint-disable */
import React from 'react';
import Icon from './Icon';

const ${name} = props => (
  <Icon {...props}>
    <svg version="1.1" viewBox="${viewBox}" width={props.iconWidth || 16} height={props.iconHeight || 16}>
      ${data}
    </svg>
  </Icon>
)

export default ${name}