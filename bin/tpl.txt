/* eslint-disable */
import React from 'react';
import Icon from './Icon';

const ${name} = props => (
  <Icon {...props}>
    <svg version="1.1" viewBox="${viewBox}" width={props.iconWidth + 'px'} height={props.iconHeight + 'px'} fill="currentColor">
      ${data}
    </svg>
  </Icon>
)

export default ${name}